# UPlayground — 프로젝트 지침

## 프로젝트 정체성
- **장르**: Path of Exile 2 / Diablo 계열 **쿼터뷰(아이소메트릭) 액션 파밍 ARPG**
- **개발 형태**: 1인 개발
- **목표 품질**: 단순 "동작하는 기능"이 아니라 **상용 게임 수준의 마스터피스**
- 엔진/기술: Unreal Engine 5.8, C++20, GAS 기반. 모듈 구조는 `PROJECT_OVERVIEW.md` 참고.

## 기능 개발 원칙 (상용 품질 기준)
기능을 구현할 때는 "요청된 동작"만 만들지 말고 아래를 기본으로 고려한다.

1. **게임 필(Game Feel)** — 타격감, 히트스톱, 카메라 셰이크, VFX/SFX 훅, 피드백 타이밍, 입력 반응성(버퍼링/캔슬). ARPG는 손맛이 핵심이다.
2. **파밍 루프 연결** — 새 기능이 전투 → 드랍 → 아이템/빌드 성장 → 더 어려운 콘텐츠 루프에 어떻게 기여하는지 의식한다.
3. **빌드 다양성 & 확장성** — 스킬·아이템·스탯은 하드코딩하지 않고 데이터 주도(DataAsset/DataTable, GameplayTag, GameplayEffect)로 설계해 조합 폭발을 수용한다.
4. **밸런싱 가능성** — 수치는 모두 데이터로 노출하고, 디자이너(=본인)가 에디터에서 빠르게 튜닝할 수 있게 한다.
5. **성능** — 다수 몬스터·투사체·드랍·데미지 텍스트가 동시에 뜨는 상황을 전제로 한다(풀링, 틱 최소화, 이벤트 기반, 가벼운 AI).
6. **UX/가독성** — 쿼터뷰 시점에서의 가독성(텔레그래프, 아이템 라벨/필터, 툴팁, 비교 UI)을 기능의 일부로 본다.
7. **엣지 케이스 & 견고성** — 사망/리스폰, 스테이지 전환, 중복 입력, 유효하지 않은 타겟, 저장/로드 상황에서도 깨지지 않게 한다.
8. **디버깅/치트 도구** — 기능마다 확인·튜닝용 치트나 디버그 표시를 함께 고려한다(`Source/UPlayground/Cheat`).

## 1인 개발 현실 반영
- 품질 목표는 높게, 범위는 **현실적으로**. 과설계보다 확장 가능한 최소 구조를 우선하고, 나중에 붙일 수 있는 확장 포인트를 남긴다.
- 반복 작업을 줄이는 **툴링·자동화·데이터 파이프라인**을 적극 제안한다.
- 제안 시 "지금 할 것 / 나중에 다듬을 것"을 구분해 우선순위를 명확히 한다.
- 레퍼런스(POE2, Diablo 3/4, Last Epoch 등)의 검증된 설계를 근거로 제시하되 그대로 베끼지 않는다.

## 작업 방식
- 기존 모듈 경계(PGAbilitySystem, PGActor, PGAI, PGData, PGMessage, PGShared, PGUI)와 기존 코드 스타일/네이밍(`PG` 접두사)을 따른다.
- 기능 구현 후에는 상용 품질 관점에서 부족한 점(연출, 폴리싱, 엣지 케이스)을 짧게 짚어준다.
- 응답은 한국어로 한다.
